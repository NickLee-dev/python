# 모듈 : 함수를 가지고 있는 python 파일
# 다른 python 파일이 가진 함수를 사용하려면, import 하면 됨
# import 모듈명 [as 줄임말]
import test_set.set_sample as ss
import test_bool.operator_sample as tos
import conditional.ifSample as ifs


# Press the green button in the gutter to run the script.
if __name__ == '__main__':
    # ss.test1()
    # ss.test2()
    # ss.test3()

    # tos.func_bool()
    # tos.func_compare()
    # tos.func_logical()
    # ifs.test_if()
    # ifs.test_even()
    # ifs.test_even2()
    # ifs.test_range()
    # ifs.test_in()
    # ifs.checkPayment()
    # ifs.checkPayment2()
    # ifs.shortCondition()
    ifs.shortCondition2()
# See PyCharm help at https://www.jetbrains.com/help/pycharm/
